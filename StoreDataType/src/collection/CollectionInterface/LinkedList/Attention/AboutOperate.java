package collection.CollectionInterface.LinkedList.Attention;

public class AboutOperate {
    /*LinkedList维护着一个双线列表（不能直接读取修改元素 && 不需要扩容，效率高）*/
    /**关于链表的添加
     *  链表的添加可以添加相同元素，从功能上看，链表具有索引，可以通过索引查找值，因此即使添加了
     *  相同元素，也可以通过索引进行定位，但是ArrayList容器和Vector容器没有索引，这就导致一旦
     *  添加相同的元素，我们无法对其进行精准定位
     * */
}
