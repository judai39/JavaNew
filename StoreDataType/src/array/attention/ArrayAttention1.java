package array.attention;

public class ArrayAttention1 {
    public static void main(String[] args) {
        //我们使用的注入int[] 这种数组是一种数组数据类型,这与Java底层中的Array类不同(对用户隐藏),以下是对Array的注解:
        //import java.lang.reflect.Array;
        //The Array class provides static methods to dynamically create and access Java arrays.
        //Array permits widening conversions to occur during a get or set operation,
        // but throws an IllegalArgumentException if a narrowing conversion would occur.

        //Array是java中最为基础的数组存储类,它仅允许存储相同类型的元素,并且也无法访问数组内部的存储情况

        //注:Arrays和Array很想,不要弄混了,Arrays是操作数组的工具类
    }
}
